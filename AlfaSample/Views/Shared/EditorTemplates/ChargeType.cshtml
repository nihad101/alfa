@model ChargeType
@using AlfaSample.Helper
@using AlfaSample.Data.Models

@(
    Html.Kendo().DropDownListFor(m => m)
           .BindTo(EnumHelpers.EnumToSelectList(typeof(ChargeType)).ToList())
           .OptionLabel(optionLabel: "Please Select")
           .SelectedIndex(0)
)