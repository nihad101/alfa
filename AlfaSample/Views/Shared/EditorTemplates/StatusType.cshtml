@model StatusType
@using AlfaSample.Helper
@using AlfaSample.Data.Models

@(
    Html.Kendo().DropDownListFor(m => m)
           .BindTo(EnumHelpers.EnumToSelectList(typeof(StatusType)).ToList())
           .OptionLabel(optionLabel: "Please Select")
           .SelectedIndex(0)
)